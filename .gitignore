# ---- Python / env ----
venv/
.venv/
__pycache__/
*.pyc
.ipynb_checkpoints/

# ---- editors / OS ----
.idea/
.vscode/
.DS_Store
Thumbs.db

# ---- archives / checkpoints (large, regenerated) ----
*.zip
models/

# ---- heavy or regenerable DATA (kept OUT of git) ----
# Source scan PDF + full-page renders:
data/scans/medical-forms-dataset.pdf
data/scans/images/
data/scans/processed/
# Per-field crops -> upload these to Google Drive instead:
data/scans/crops/
# Fine-tuning set is rebuilt on whichever machine trains:
data/train_recog/
# Per-run outputs (regenerated by the notebooks):
data/generated/predictions_*.csv
data/generated/split_train_val.json

# ---- intentionally KEPT in git (small, needed to run from a clone) ----
#   data/template/field_map.json
#   data/scans/labels.csv
#   data/scans/preprocess_report.json
#   data/vocab/**
#   data/generated/patient_db.json , validation_forms.csv
